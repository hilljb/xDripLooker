# ============================================================
# Prefer conda for all installs (see environment.yml):
#   conda env create -f environment.yml
#   conda activate xdrip
#
# This file is provided as a pip fallback ONLY for environments
# where conda is not available.
# ============================================================

# MongoDB driver + DNS for mongodb+srv:// URIs
pymongo==4.16.0
dnspython==2.8.0

# Data analysis
pandas==3.0.1
